[project]
name = "refactor-envars"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "argparse>=1.4.0",
    "boto3>=1.39.3",
    "jinja2>=3.1.6",
    "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
    "invoke>=2.2.0",
    "pre-commit>=4.2.0",
    "pytest>=8.4.1",
]
